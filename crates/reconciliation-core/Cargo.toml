[package]
name = "reconciliation-core"
version = "0.1.0"
edition = "2021"
description = "Core Domain Models and Reconciliation Engine for Offline Accounting Desktop"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
