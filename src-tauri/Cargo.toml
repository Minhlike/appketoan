[package]
name = "appketoan"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

[lib]
name = "tauri_app_lib"
crate-type = ["staticlib", "rlib"]
# The GNU WebView2 loader cannot execute a synthetic lib-test harness reliably.
# This crate has no unit tests; pure business tests stay in reconciliation-core
# and Tauri behavior is covered by build + release executable smoke gates.
test = false
doctest = false

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
reconciliation-core = { path = "../crates/reconciliation-core" }
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
rust_decimal = { version = "1.36", features = ["serde", "serde-with-str"] }

[dev-dependencies]
rust_decimal_macros = "1.36"
