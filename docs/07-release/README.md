# Release Engineering & Packaging

## Packaging Strategy
- Production packaging via Tauri 2 bundler.
- Output: Windows Standalone Executable / MSI Installer in `releases/`.
- Zero runtime prerequisites for end-users (no Node.js, Rust, Python, Docker required).
